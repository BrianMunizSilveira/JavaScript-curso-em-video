function somarStrings(numero1, numero2) {
    // Conversão de String para Number
    let num1 = Number(10);
    let num2 = Number(20);
    let soma = num1 + num2;
    return soma;
}

// Teste a função
console.log(somarStrings("10", "20")); // Deve retornar 30
