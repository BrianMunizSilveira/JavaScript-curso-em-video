let numeros = [1, 2, 3, 4, 5] // Criando o array 'numeros' com os valores [1, 2, 3, 4, 5].

for (let i = 0; i < numeros.length; i++) /* Para a variável 'i', que recebe o valor 0 como inicial, enquanto 'i' for menor que o comprimento do array 'numeros', execute o bloco de código e, após cada iteração, incremente o valor de 'i' em 1. */  {
    console.log(numeros[i]) // Exibindo no console o valor do elementom do array 'numeros' correspondente ao índice atual 'i'.
}